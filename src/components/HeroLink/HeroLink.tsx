import { FC } from "react";

import "./heroLink.scss";
const HeroLink: FC = () => {
  return (
    <a className="hero_link" target="_blank" href="https://web.whatsapp.com/">
      JOIN WHATSAPP
    </a>
  );
};

export default HeroLink;
